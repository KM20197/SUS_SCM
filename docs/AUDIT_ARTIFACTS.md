# Audit artifacts

Do not commit large artifacts to git history.

Distribute via:
- GitHub Releases (attach `*_AUDIT.tar.gz` and `.sha256`), and/or
- an archival repository with immutable identifiers.

Include a short MANIFEST describing command line and run ID.
